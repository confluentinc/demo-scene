{
  "name": "syslog-tcp",
  "config": {
    "tasks.max": "1",
    "connector.class": "io.confluent.connect.syslog.SyslogSourceConnector",
    "kafka.topic": "syslog",
    "syslog.port": "42515",
    "syslog.listener": "TCP",
    "syslog.reverse.dns.remote.ip": "true"
  }
}