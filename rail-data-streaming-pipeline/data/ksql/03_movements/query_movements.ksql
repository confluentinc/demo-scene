SELECT TIMESTAMPTOSTRING(ROWTIME, 'yyyy-MM-dd HH:mm:ss') as ACTUAL_TIMESTAMP,
       EVENT_TYPE,
       LOC_NLCDESC,
       PLATFORM,
       VARIATION ,
       TOC,
       TRAIN_ID, 
       GEOHASH, 
       GEO_OSM_URL
  FROM TRAIN_MOVEMENTS_01;
  